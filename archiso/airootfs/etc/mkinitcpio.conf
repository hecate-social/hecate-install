# mkinitcpio configuration for hecatOS live ISO
# The archiso hooks are required for the live environment to boot

MODULES=()
BINARIES=()
FILES=()
HOOKS=(base udev modconf archiso archiso_loop_mnt kms block filesystems keyboard)
