-- Ensure you have a plugin manager like 'packer.nvim'
-- First, install 'packer.nvim' if you haven't already

-- Include this in your init.lua
require('packer').startup(function(use)
  -- Other plugins...

  -- Add rest.nvim
  use {
    'rest-nvim/rest.nvim',
    requires = { 'nvim-lua/plenary.nvim' },
  }

  -- Other plugins...
end)

-- Optional: Configure rest.nvim
require('rest-nvim').setup({
  -- Configuration options go here
})
